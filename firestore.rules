rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ログイン済みユーザーのみ読み書き可能。
    // サインアップ画面は用意していないため、ユーザーはFirebase Consoleの
    // Authentication画面から手動で作成する運用を想定している。
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
